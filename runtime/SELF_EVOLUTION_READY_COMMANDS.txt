SELF-EVOLUTION CYCLE COMPLETE — REAL 1-MONTH XAU OVERNIGHT ARTIFACT (2026-05-28)

Artifact used:
  - validation_campaign_val_20260528_145148_overnight_1m
  - standardized_validation_XAUUSDm_4w_...

Key signals extracted by MetaOptimizer + RetrainingOrchestrator:
  - candidate_beats_champion: true (+8910 pnl delta)
  - Strong edges: double_bottom (+5824), low_news_prox (+7758), double_top (+2251), breakout_up
  - TimeExitSpec: candidate delivered first positive max_hold segment (+64.13)
  - Verdict: ITERATE_FURTHER (82%) — promote=false for now

CONCRETE OVERRIDES FOR NEXT DECISION PPO (persisted + TUI-visible):
  reward_profile: hardened
  penalty_scale: 0.90
  feature_importance_overrides:
    patterns: 1.29
    timing: 1.38
    news_proximity: 1.15
  ensemble_weight_deltas:
    classical: +0.04
    rainforest: +0.02
    ppo: -0.03
  top_boost_patterns: [double_bottom, double_top, breakout_up, bullish_engulfing]

Stable pointer file (for launchers):
  runtime/next_training_overrides.json   ← copy of the enhanced overrides above

RECOMMENDED NEXT ACTION (from the full_cycle agent):
  Launch a Decision PPO retrain on XAUUSDm consuming these overrides:
    python -m Python.autonomous.run_cycle --symbol XAUUSDm --mode promotion_gates --timesteps 350000

  Or (if using the classic launcher with env/override support):
    $env:TRAINING_OVERRIDES_FILE="runtime/next_training_overrides.json"
    .\.venv312\Scripts\python.exe scripts/launch_decision_ppo_training.py --symbol XAUUSDm --timesteps 350000

  After retrain: run a fresh 4w fast validation campaign (scripts/run_overnight_validation.py --symbols XAUUSDm --months 1 --speed fast)
  Then let the supervisor / orchestrator auto-evaluate via the same full-cycle path.

These overrides were synthesized directly from the real harness pattern_profitability + timing_analysis + time_exit_effectiveness on the overnight artifact (not just the conservative auto-parser).

TUI: The new "SELF-EVOLUTION / META OVERRIDES" panel in mini_pipeline_tui.py (and monitor_tui --mini) now shows this live.
  Run: .\.venv312\Scripts\python.exe scripts\mini_pipeline_tui.py
  Or the richer: .\.venv312\Scripts\python.exe scripts\monitor_tui.py --mini

Full detailed report from the spawned agent:
  runtime/agent_status/full_cycle_with_real_overnight_artifact_20260528.json

Loop status: CLOSED and operational on real data. Next cycle will see the updated overrides + EM seeding from these attributions.

Generated by the dedicated "full cycle with this artifact" subagent (ID 019e6f27-9ff3-78f2-ba78-28fa66924154) + direct follow-up wiring.
