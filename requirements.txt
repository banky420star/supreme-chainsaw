# Chain Gambler dependencies — pinned minimum versions for reproducibility
# Last updated: 2026-05-27  (Python 3.13 compatible) - added bottle for api_server

# Core data & ML
numpy>=2.0.0
pandas>=2.2.0
scikit-learn>=1.4.0
torch>=2.2.0

# Deep RL
gymnasium>=0.29.0
stable-baselines3[extra]>=2.3.0

# Features & backtesting
pandas_ta>=0.3.14
vectorbt>=0.26.0

# Data sources
yfinance>=0.2.35
metaapi-cloud-sdk>=24.0.0
dukascopy-python>=0.2.0

# API & async
aiohttp>=3.9.0
requests>=2.31.0

# Config & serialization
pyyaml>=6.0.1
pydantic-settings>=2.1.0

# Logging
loguru>=0.7.0

# Caching & storage
redis>=5.0.0
polars>=0.20.0
pyarrow>=15.0.0
openpyxl>=3.1.0

# Optimisation
optuna>=3.5.0
joblib>=1.3.0

# RPyC bridge (critical — keep in sync with Wine Python)
rpyc>=6.0.0

# Concurrency
filelock>=3.13.0

# Web / API server (api_server.py uses Bottle; geventwebsocket is optional for WS)
bottle>=0.12.25
# geventwebsocket>=0.10.1   # optional, for advanced WebSocket support in dashboard

# Explainability
xai-sdk>=0.5.0

# Platform-specific
MetaTrader5; sys_platform == 'win32'
